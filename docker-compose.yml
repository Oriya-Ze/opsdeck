services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: opsdeck
      POSTGRES_PASSWORD: opsdeck
      POSTGRES_DB: opsdeck
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U opsdeck -d opsdeck"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://opsdeck:opsdeck@postgres:5432/opsdeck
      CORS_ORIGINS: '["http://localhost:3000","http://127.0.0.1:3000"]'
      ENCRYPTION_KEY: opsdeck-dev-key-change-in-production-32b=
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request as u; u.urlopen('http://127.0.0.1:8000/api/ready', timeout=5)\""]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 40s

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://127.0.0.1:3000/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  postgres_data:
