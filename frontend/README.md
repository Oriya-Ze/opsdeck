# OpsDeck Frontend

React + Vite + TypeScript + Tailwind CSS dashboard for OpsDeck.

## Local Development

```bash
npm install
npm run dev
```

App runs at http://localhost:3000 with API proxied to the backend.
